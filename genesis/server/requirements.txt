# Core dependencies
genesis-world
numpy

# PyTorch (required by Genesis)
# For CPU only (smallest install):
torch>=2.0.0

# For AMD GPU (ROCm):
# Install separately: pip install torch --index-url https://download.pytorch.org/whl/rocm5.7

# For NVIDIA GPU (CUDA):
# Install separately: pip install torch --index-url https://download.pytorch.org/whl/cu121

# Video streaming server
flask>=2.0.0
opencv-python
pillow
